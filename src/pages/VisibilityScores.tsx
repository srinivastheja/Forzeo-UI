import React from 'react';
import { Typography } from '@mui/material';

const VisibilityScores: React.FC = () => (
    <div>
        <Typography variant="h4" sx={{ mb: 2 }}>Visibility Scores</Typography>
        <Typography color="text.secondary">Visibility and ranking scores for assets.</Typography>
    </div>
);

export default VisibilityScores;
