import React from 'react';
import { Typography } from '@mui/material';

const Citations: React.FC = () => (
    <div>
        <Typography variant="h4" sx={{ mb: 2 }}>Citations</Typography>
        <Typography color="text.secondary">Track content citations and references.</Typography>
    </div>
);

export default Citations;
