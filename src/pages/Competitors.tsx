import React from 'react';
import { Typography } from '@mui/material';

const Competitors: React.FC = () => (
    <div>
        <Typography variant="h4" sx={{ mb: 2 }}>Competitors</Typography>
        <Typography color="text.secondary">Compare with competitor data and trends.</Typography>
    </div>
);

export default Competitors;
