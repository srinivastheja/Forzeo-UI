import React from 'react';
import { Typography } from '@mui/material';

const PromptExplorer: React.FC = () => (
    <div>
        <Typography variant="h4" sx={{ mb: 2 }}>Prompt Explorer</Typography>
        <Typography color="text.secondary">Search and manage prompts.</Typography>
    </div>
);

export default PromptExplorer;
